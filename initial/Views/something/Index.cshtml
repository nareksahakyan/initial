@model IEnumerable<initial.Models.something>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.someName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.someIDNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.someName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.someIDNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.somethingid }) |
            @Html.ActionLink("Details", "Details", new { id=item.somethingid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.somethingid })
        </td>
    </tr>
}

</table>
